node_modules
dist
.env
*.local
npm-debug.log*

# PNGs gerados por slide (routes/renderImage.ts) -- nao faz sentido versionar.
generated/

# Tokens da integração Canva (refresh token rotativo) -- nunca versionar.
.canva-tokens.json
