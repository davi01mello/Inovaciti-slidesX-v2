/**
 * A curva da casa pro fluxo de login: um ease-out expressivo (parte rápido,
 * assenta macio) usado em TODAS as peças da coreografia: entrada da tela, troca
 * de passo do campo e dissolução da cortina. Uma curva só é o que faz o fluxo
 * inteiro parecer um gesto contínuo em vez de três animações soltas.
 */
export const EASE = [0.16, 1, 0.3, 1] as const;
